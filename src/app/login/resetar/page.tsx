import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Resetar a senha | Dogs',
  description: 'Resete sua senha do site Dogs.',
};

export default async function ResetarPage() {
  return (
    <main>
      <h1>Resetar</h1>
    </main>
  );
}
